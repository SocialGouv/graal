{{- if .Values.externalSecrets.enabled }}
{{- range $i, $secret := .Values.externalSecrets.secrets }}
{{- if $i }}
---
{{- end }}
{{ include "graal.externalSecret" (dict "root" $ "secret" $secret) }}
{{- end }}
{{- end }}
