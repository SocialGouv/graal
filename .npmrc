# pnpm v10 supply-chain hardening
# Enforce pnpm trust policy for dependency build scripts
# (pnpm will only allow build scripts for deps listed in `pnpm.onlyBuiltDependencies`)
strict-dep-builds=true
